// 2. Accept one number from user and print that number of * on screen. 

function Display(no){
    while(no > 0)
    {
        console.log(" * ");
        --no;
    }
}
// let num = prompt("Enter any number :");
// Display(num);

Display(5);